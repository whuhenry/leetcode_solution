#include <vector>
using namespace std;

class Solution {
public:
    int numFriendRequests(vector<int>& ages) {
        int count = ages.size();
        if (2 > count) {
            return 0;
        }
        int total = count;
        for (int i = 0; i < count; ++i) {
            for (int j = i + 1; j < count; ++j) {
                if ()
            }
        }
    }
};